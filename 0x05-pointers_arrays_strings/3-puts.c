#include "main.h"
/**
 * _puts - prints string with new line
 * @str: string
 * Return: void
 */

void _puts(char *str)
{
	int a = 0;

	while (str[a])
	{
		_putchar(str[x]);
		x++;
	}
	_putchar('\n');
}
